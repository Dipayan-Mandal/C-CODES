#include <stdio.h>
#include <string.h>
int main()
{
	int a[]={3,8,14,4,54,64};
	int i,n;
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		if(a[i]==n)
		printf("data is in index of %d",i);
		break;
		if(a[i]*!=n)
	printf("data is not present");
	}
	return 0;
}
